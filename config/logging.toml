# Configuração de logging (rotação de arquivos em logs/)
# Aplicada por uvicorn/aplicação quando presente.

[loggers.creditops]
level = "INFO"

[handlers.file]
class = "logging.handlers.RotatingFileHandler"
filename = "logs/creditops.log"
maxBytes = 10485760   # 10 MB
backupCount = 10
